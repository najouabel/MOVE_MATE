import { DateSubstractorPipe } from './date-substractor.pipe';

describe('DateSubstractorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSubstractorPipe();
    expect(pipe).toBeTruthy();
  });
});
